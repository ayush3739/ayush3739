<?xml version="1.0" encoding="UTF-8"?>
<svg width="1600" height="400" viewBox="0 0 1600 400" xmlns="http://www.w3.org/2000/svg" role="img" aria-labelledby="title desc">
  <title id="title">Ayush Maurya — Profile Banner</title>
  <desc id="desc">Dark gradient banner with refreshing blue accents and bold greeting + role subtitle.</desc>

  <defs>
    <!-- Background gradient -->
    <linearGradient id="bg" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#0b0b0b"/>
      <stop offset="100%" stop-color="#0f172a"/>
    </linearGradient>

    <!-- Subtle grid dots -->
    <pattern id="dots" width="20" height="20" patternUnits="userSpaceOnUse">
      <circle cx="1.5" cy="1.5" r="1.5" fill="#ffffff"/>
    </pattern>
  </defs>

  <!-- Background -->
  <rect width="1600" height="400" fill="url(#bg)"/>

  <!-- Subtle grid overlay -->
  <rect x="0" y="0" width="1600" height="400" fill="url(#dots)" opacity="0.03"/>

  <!-- Geometric accents (user-selected) -->
  <!-- Bottom-left overlapping circles -->
  <circle cx="0" cy="400" r="110" fill="#22D3EE" opacity="0.92"/>  <!-- Light Sky -->
  <circle cx="150" cy="400" r="130" fill="#1E3A8A" opacity="0.90"/> <!-- Blue Accent -->

  <!-- Top-right big circle -->
  <circle cx="1480" cy="-40" r="230" fill="#60A5FA" opacity="0.88"/> <!-- Dark Blue -->

  <!-- Text styles -->
  <style>
    .title { font-family: "Segoe UI", "Inter", "Poppins", system-ui, -apple-system, "Helvetica Neue", Arial, sans-serif; font-weight: 900; font-size: 120px; }
    .subtitle { font-family: "Segoe UI", "Inter", "Poppins", system-ui, -apple-system, "Helvetica Neue", Arial, sans-serif; font-weight: 500; font-size: 42px; }
  </style>

  <!-- Greeting with layered outline effect -->
  <!-- Back outline -->
  <text x="170" y="210" class="title" fill="none" stroke="#ffffff" stroke-width="8" opacity="0.18">Hi, I'm Ayush 👋</text>
  <!-- Offset outline -->
  <text x="176" y="216" class="title" fill="none" stroke="#ffffff" stroke-width="6" opacity="0.35">Hi, I'm Ayush 👋</text>
  <!-- Fill -->
  <text x="170" y="210" class="title" fill="#ffffff">Hi, I'm Ayush 👋</text>

  <!-- Subtitle -->
  <text x="800" y="275" class="subtitle" fill="#ffffff" opacity="0.9" text-anchor="middle">
    Student Developer • Python Learner • AI/ML Explorer
  </text>
</svg>